provider "aws" {
  region = "us-east-1"
  # access_key = "YOUR_ACCESS_KEY"
  # secret_key = "YOUR_SECRET_KEY"
  # token = "YOUR_SESSION_TOKEN"

  # You can also use a named profile from the AWS CLI credentials file.
  # profile = "your_profile_name"
}
